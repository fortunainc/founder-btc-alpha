name: secret-guard
on:
  push:
    branches: ['**']
  pull_request:
jobs:
  secret-guard:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - name: secret-presence guard
        run: bash scripts/ci-secret-guard.sh
